# Pattern class: iac
#
# Infrastructure as code — CDK and Terraform topology, stack and module
# boundaries, account and environment layout, deploy-pipeline shape, and the
# policy/cost gates a change passes before apply.
#
# Empty-but-present marker. See ../INDEX.md for the page template, the row
# format, and the topic admission rule.