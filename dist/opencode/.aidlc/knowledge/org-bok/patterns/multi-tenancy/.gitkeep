# Pattern class: multi-tenancy
#
# Tenant identity, isolation, and per-tenant data boundaries — how a tenant is
# established from the request, how a query is prevented from crossing tenants,
# and where the isolation boundary sits (row, schema, database, account).
#
# Empty-but-present marker. See ../INDEX.md for the page template, the row
# format, and the topic admission rule.