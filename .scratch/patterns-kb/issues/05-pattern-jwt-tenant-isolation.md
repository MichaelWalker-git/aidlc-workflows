# 05 — Pattern: multi-tenancy/jwt-tenant-isolation (draft)

**What to build:** The org's multi-tenant JWT isolation decision as a pattern page: code-read of the JWT claims path in the relevant exemplar repos → draft → indexed. Explicitly ships as `draft` — NOT blocked on the Architect's Service Delivery Platform audit (ADR-003/005: the audit later blesses or amends the file; agents get calibrated-trust guidance now).

**Blocked by:** 01, 02.

**Status:** ready-for-agent

- [ ] Page follows the template: frontmatter (`status: draft`, `reviewed`, `owner`), precedence header, all five sections
- [ ] "Our approach" states the org decision extracted from the actual claims-path code; satisfies the topic admission rule
- [ ] Exemplars section cites profiles via relative links (Service Delivery Platform among them)
- [ ] INDEX.md row added
- [ ] Shape test, packager, and parity check green